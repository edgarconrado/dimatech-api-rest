# ------------------------------------------
# MySQL DATABASE CONFIGURATION FOR Dimatech
# ------------------------------------------
spring:
    datasource:
        url: jdbc:mysql://localhost/db_dimatech
        username: dimauser
        password: Sh3l1t4s
        driverClassName: com.mysql.jdbc.Driver
    jpa:
        database-platform: org.hibernate.dialect.MySQL57Dialect  
        hibernate:
            ddl-auto: create-drop
logging:
    level:
        org:
            hibernate:
                SQL: debug        